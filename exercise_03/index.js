//import 'generateRandomNumberInRange' and execute
import generateRandomNumberInRange from "./generateRandomNumberInRange";

const randomNumber = generateRandomNumberInRange(10);

console.log(`Random number within the range of 10: ${randomNumber}`);
